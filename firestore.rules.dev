rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Reglas SIMPLIFICADAS para desarrollo
    match /expenses/{expense} {
      // Permitir todo si el usuario está autenticado
      allow read, write: if request.auth != null;
    }
  }
}

